# خارطة الطريق — وكيل محمود

آخر تحديث: 3 أغسطس 2026 · الإصدار الحالي **0.7**

---

## ✅ منفَّذ

| # | البند | الإصدار |
|---|---|---|
| 1 | تصحيح عدّاد الأحداث (كان يعدّ أسطر العرض لا الدفتر) | 0.2 |
| 2 | فتح البوابة من صفر فورياً — مهلة 4 ساعات للانقلاب وحده | 0.3 |
| 3 | انقلاب البوابة يشدّ الستوب لنقطة الدخول ولا يغلق | 0.3 |
| 4 | الطابور — إشارة مرفوضة بسبب البوابة تُحفظ 45 دقيقة | 0.4 |
| 5 | توثيق الإشارة المكررة (سعر · فارق · زمن · حكم مبدئي) | 0.5 |
| 6 | **بوابتان مستقلتان** — `🚦HA` هيكن · `🚦RAW` خام | 0.6 |
| 7 | **مقاعد منفصلة** — 2 هيكن · 2 خام · 1 استكشاف | 0.6 |
| 8 | **دفتر الظل** — كل إشارة تُسجَّل ولو رُفضت، مع سعر ما بعد 4 ساعات | 0.6 |
| 9 | التعارض بين البوابتين يُفتح ويُسجَّل بدل أن يُهمل | 0.6 |
| 10 | **إصلاح فقدان الحالة عند إعادة النشر** ⭐ | 0.7 |
| 11 | عرض `الإصدار` صار يقرأ `CONFIG_VERSION` بدل نص ثابت | 0.7 |

### تفصيل البند 10 — العطب وإصلاحه (3 أغسطس)

**العَرَض:** خمس صفقات مفتوحة (SUI · HYPE · NEAR · APT · BCH) اختفت بلا أي حدث
`CLOSE` بعد إعادة نشر، والرصيد رجع 50.0، والطابور من 95 إلى صفر.

**السبب الجذري:** `save()` كانت تحوّل حقلَي وقت فقط (`gate_since` · `pending_since`)،
وحقلا بوابة هيكن الجديدان (`gate_ha_since` · `pending_ha_since`) يبقيان `datetime`
فيكسران `json.dump`. والأخطر أن `open(STATE_F,"w")` تفرّغ الملف **قبل** أن يفشل
الحفظ — أي أن كل عملية حفظ كانت تمسح الحفظة السابقة.

**العطب وُلد مع بوابة هيكن في 0.6.** قبلها كان الحفظ سليماً.

**الإصلاح — أربع نقاط:**

1. حقول الوقت الأربعة كلها تُحوَّل نصاً عبر `TS_FIELDS`
2. `positions` تُنسخ بدل تعديل القاموس الحيّ في الذاكرة
3. **كتابة ذرّية** (`.tmp` + `os.replace`) — الفشل لا يفرّغ الملف القديم
4. `opened_at` يرجع `datetime` عند التحميل

**التحقق:** مُثِّلت إعادة تشغيل على النسختين.
القديمة: `save error: Object of type datetime is not JSON serializable` → الصفقة ضاعت.
الجديدة: الصفقة نجت · `opened_at` من نوع `datetime` · الرصيد 48.715 محفوظ.

⚠️ **لم يُختبر بعد على السيرفر بصفقة حقيقية.** الدليل النهائي يجي عند أول
إعادة نشر تصادف صفقة مفتوحة.

---

## ⏳ الوضع الحالي (3 أغسطس · 17:22)

| البند | القيمة |
|---|---|
| الإصدار المعروض | **0.7** ✅ |
| البوابتان | مقفولتان صباحاً → **LONG BTC فقط** مساءً |
| تعارض البوابتين | `false` — متفقتان |
| المقاعد | 0/2 هيكن · 0/2 خام · 0/1 استكشاف |
| صفقات مفتوحة | صفر |
| الطابور | صفر |
| الدفتر | 1234 حدثاً — نجا من كل إعادات النشر ✅ |
| التخزين | 💾 دائم على `/var/data` — مؤكد |

**دفتر الظل يعمل فعلياً:** كل `queued` يقابله `shadow`.

### ملاحظات على العيّنة

- **عيّنة الخمس صفقات ضاعت** (2 أغسطس) بسبب البند 10. تُسقَط من القياس.
- `CONFIG_VERSION = "0.7"` كان مكتوباً في الكود منذ 2 أغسطس، والدفتر
  يُختم به صح من السطر 199. **الخطأ كان في العرض فقط** — فالعيّنة مفصولة سليمة.
- ⚠️ `MAX_DAILY_TRADES` و`MAX_OPEN` رُفعا من 3 إلى **5** في 2 أغسطس،
  أي في اليوم الثاني من أسبوع المراقبة. نصفا الأسبوع لا يُقارنان مباشرة.

---

## 🎯 قاعدة الحسم بين البوابتين — مقفلة مسبقاً

```
40 قيد ظل لكل بوابة (أو 14 يوماً)
المقياس: حركة السعر في الاتجاه المسموح بعد 4h
فارق ≥10 نقاط → الفائز يُعتمد والخاسر يُحذف
فارق <10  → هيكن (أبسط) ويُعاد القياس
```

---

## 📋 البنود المؤجلة — بالترتيب

### 1. ربط تنبيهات «القاع الخام» و«القمة الخام»  ⭐ الأولوية القصوى

منفَّذة في `Master_hunter_v15` **بلا رابط ويبهوك** — أي ميتة عملياً.

**سبب وجودها:** الزناد 3/3 مستحيل بنيوياً عند القاع. شرطه الثاني
`trig_early_up = btc_early and btc_d1h==1` يقرأ BTC على 1h ويتطلب
ارتفاع السعر فعلاً، فيصل متأخراً.

**دليل ZEC:**

| الحدث | السعر |
|---|---|
| دخول محمود اليدوي | 464.48 |
| Early MA36 · 04:15 | 465.90 |
| زناد Master · 09:15 | ~474 |

تأخر **5 ساعات و2%**. القاع الخام (`trig_accum_buy and trig_at_bot and
not liquidity_exit` — شرطان لا ثلاثة) هو العلاج.

---

### 2. تنبيهات BTC التسعة الباقية

موجودة على الشارت بلا رابط ويبهوك:

```
الامتصاص عند القاع · الامتصاص عند القمة
VAL · VAH
التركيبة الناضجة لونج · شورت
Spring / توزيع مخفي
خروج السيولة (BTC↓+BTC.D↓)  ← يصل حالياً كفيتو داخلي
```

**ملاحظة (3 أغسطس):** Spring المخفي **لا عمود له في لوحة Master** — تنبيه فقط.
شرطه: `ha_bull + squeeze_count ≥ 2 + abs_buy`، يُقرأ بجمع ثلاثة صفوف
(🕯️ هيكن · 🔥 Squeeze · 🧲 الامتصاص). العمود المستقل موجود في
Wyckoff v3.4 وحده، وشرطه هناك هيكن **1D** لا الفريم المفتوح.

---

### 3. إصلاح SHIB — زناد قمة مكرر · قاع مفقود

تشخيص، لا يعطّل غيره.

---

### 4. CVD كمكوّن خامس في `score_calc`

`comp = pos + mom + vsc + tsc` — أربع دوال متوسطات، صفر مكوّنات لحظية،
وتقرأ `close` فقط (صفر استخدام لـopen/high/low). فتعطي شورت على شمعة
خضراء بدلتا موجبة.

**الوزن 1 لا 2** — لأن CVD في السكربت تقديري. أثره يظهر عند `B = −2` تحديداً.

⚠️ يُنفَّذ **بعد** أن يجمع دفتر الظل عيّنة كافية — تعديل القرار الآن يلوّث القياس.

---

### 5. حدّ منفصل للطابور

ظهر في الاختبار: الطابور يستهلك الحد اليومي في لحظة واحدة عند فتح البوابة.
بلغ **95 إشارة** في 3 أغسطس قبل الانقلاب.

**القرار:** راقب أولاً. الحل — إن لزم — حدّ مستقل للطابور لا تعديل الحد اليومي.

---

### 6. لوحة Master كاملة كبيانات  ⭐⭐ المشروع الكبير

**الهدف:** أن يرى الوكيل ما تراه أنت على اللوحة — لا أحداثاً متفرقة.

**الطريقة:** تنبيه واحد لكل عملة يحمل اللوحة كلها JSON:

```json
{"tk":"BTCUSDT.P","tf":"240","px":63054.9,"rsi":39,
 "cvd":-23340,"cvd_tr":-1,"rsi_tr":-1,"tools":0,
 "trig":0,"val":64770,"pos_val":-0.5,"absorb":0,
 "squeeze":0,"trap_long":1,"veto":1,"decision":0,
 "oi":3.38,"adx":26.7,"alts":-1}
```

**ثلاث قطع:** تعديل `Master_hunter.pine` · إعداد التنبيهات · **طبقة قرار
جديدة** — وهذه هي العمل الحقيقي، ترجمة قراءتك للوحة إلى شروط صريحة.

**لماذا مؤجَّل:** قد يتبيّن بعد البندين 1-2 أن الطبقات الحالية تكفي.

---

### 7. فحص عمق التصحيح  ⚠️ خلاف مفتوح

**موقف محمود:** القاع لا يحتاج علماً — المرساة واضحة (دعم · MA36 · ذروة بيع).

**موقف كلود:** الوكيل لا يرى ما تراه، فيعامل إشارة بعد هبوط 8% مثل إشارة
بعد صعود متواصل.

**الحسم:** السجل، لا الجدال. **عائق تقني:** يحتاج RSI أو مسافة السعر من
القاع، ولا يصل الوكيل أيٌّ منهما — فهو مرتبط بالبند 6.

---

### 8. الإشارة المكررة — من توثيق إلى فعل

تُوثَّق الآن (`repeat_signal`) وتُرفض. بعد جمع العيّنة، افحص كل سطر:

- `verdict: تأكيد محتمل` (سعر أفضل + بعد ساعة+) = كان تأكيداً وضاع
- `verdict: ⚠️ سكين محتملة` (سعر أسوأ + خلال 30د) = الرفض أنقذك

العتبات الحالية (0% · 60د · −0.5% · 30د) مبدئية — تُعدَّل على البيانات الفعلية.

---

## 🔌 Pionex API — مسار منفصل

⚠️ **Futures API بدعوة فقط.** ابدأ بطلب الصلاحية (النص الجاهز في
`claude_AGENT_STATUS.md`).

**التصميم جاهز:** `PaperBroker` طبقة منفصلة — إضافة Pionex = استبدال
كلاس واحد بلا مساس بباقي الكود.

**المفاتيح لا تُكتب في الكود ولا تُشارك** — Environment في Render فقط.

---

## 🚫 قواعد ثابتة لا تُخالَف

- **لا تنفيذ حقيقي قبل أسبوع مراقبة كامل**
- الحكم على السباكة لا على الربح
- 100 صفقة لا تكفي — الرقم الحقيقي ~400
- الحدود الحالية: $1 · 25x · **5 صفقات يومياً · 5 مفتوحة** · إيقاف عند −30%
- **لا تعديل بلا بيانات.** كل بند هنا يُحسم بالسجل لا بالإحساس.
- ⚠️ **أي إعادة نشر تُوثَّق بوقتها** — لفصل ما قبلها عمّا بعدها في القياس.

---

## قاعدة الخروج المعتمدة (31 يوليو)

مطبَّقة في الكود، لا تُعدَّل قبل 30 صفقة موثّقة:

1. بلوغ TP حسب الرافعة → **أغلق النصف فوراً بلا تفكير**
2. انقل SL للنصف الباقي إلى **نقطة الدخول** — لا يمكن أن تخسر بعدها
3. الباقي يخرج على: انعكاس هيكن · أو زناد قمة 3/3 · أو انقلاب البوابة

⚠️ **ممنوع تحريك SL للأسفل بعد المرحلة 2** — هذا هو العطب نفسه الذي
بُنيت القاعدة لمنعه.
