# خارطة الطريق — وكيل محمود

آخر تحديث: 1 أغسطس 2026 (11:50) · الإصدار الحالي 0.5

---

## ✅ منفَّذ

| # | البند | الإصدار |
|---|---|---|
| 1 | تصحيح عدّاد الأحداث (كان يعدّ أسطر العرض لا الدفتر) | 0.2 |
| 2 | فتح البوابة من صفر فورياً — مهلة 4 ساعات للانقلاب وحده | 0.3 |
| 3 | انقلاب البوابة يشدّ الستوب لنقطة الدخول ولا يغلق | 0.3 |
| 4 | الطابور — إشارة مرفوضة بسبب البوابة تُحفظ 45 دقيقة | 0.4 |
| 5 | توثيق الإشارة المكررة (سعر · فارق · زمن · حكم مبدئي) | 0.5 |

---

## ⏳ الوضع الحالي

**أسبوع المراقبة بدأ 1 أغسطس.**

- 25 عملة · 211 تنبيهاً · الويبهوك مؤكد الوصول
- التخزين دائم على `/var/data` — مؤكد
- **البوابة لم تُفتح ولا مرة بعد** — Master يعطي STAYOUT
- الطابور اشتغل فعلياً أول مرة عند 07:15

**المنتظر:** أول سطر `gate` · أول `OPEN` · أول `repeat_signal`

---

## 📋 البنود المؤجلة — بالترتيب

### 1. ربط تنبيهات BTC التسعة  ⭐ الأولوية القصوى

موجودة على الشارت **بلا رابط ويبهوك**:

```
الامتصاص عند القاع · الامتصاص عند القمة
VAL · VAH
التركيبة الناضجة لونج · شورت
Spring / توزيع مخفي
خروج السيولة (BTC↓+BTC.D↓)  ← يصل حالياً لكن كملاحظة فقط
```

**لماذا أولاً:** جاهزة تماماً، تحتاج رابطاً فقط. تنقل الوكيل من
طبقة واحدة (البوابة) إلى أربع (بوابة + فيتو + منطقة سعر + امتصاص).

---

#### 1-أ · الفيتو يصير فيتو فعلياً

**الوضع الآن:** `BTC↓+BTC.D↓` يصل ويُسجَّل `veto_state` — **ولا يمنع
شيئاً**. اليوم رُفضت الإشارات لأن البوابة مقفولة أصلاً، لا بسبب الفيتو.

⚠️ **الخطر:** لو فتح Master البوابة LONG وخروج السيولة قائم، الوكيل
**سيدخل لونج** والفيتو مكتوب في السجل يتفرّج.

**المطلوب:** حالة `veto_active` في `State`، تُفحص داخل `vetoes()`
قبل البوابة، وترجع سبباً مثل `liquidity_exit`.

**فلسفة الفيتو (محمود · 1 أغسطس):** فيتو **حياد** لا فيتو اتجاه —
لونج متأخر ممنوع · شورت متأخر ممنوع. يمنع الاثنين معاً لأن خروج
السيولة يعني سيولة رقيقة، والسوق الرقيق يصطاد الستوب في الاتجاهين.

---

#### 1-ب · انتهاء الفيتو  ⚠️ ثغرة مكتشَفة

**المشكلة:** الفيتو يُطلق حين يتحقق، ولا يُطلق شيء حين ينتهي. فمتى
يُرفع؟ لا الوكيل يعرف ولا اللوحة تقول.

**الخطر:** فيتو بلا نهاية قد يقفل النظام أياماً بلا أن تدري.

**حلان (أيهما أو كلاهما):**
- تنبيه صريح «انتهى خروج السيولة» من السكربت
- مهلة زمنية: يُرفع تلقائياً بعد إغلاق 4h واحد بلا تكرار الشرط

---

#### 1-ج · الطابور ينتظر رفع الفيتو  ⭐ فكرة محمود

**الفكرة:** خروج سيولة والاتجاه غير معروف → الإشارات تنتظر في
الطابور → حين يُرفع الفيتو وتفتح البوابة، تدخل حسب اتجاه السوق.

**ثلاثة عوائق في الكود الحالي:**

1. **الفيتو لا يُدخل الطابور** — الطابور يستقبل `gate_closed` و
   `against_gate` فقط. يحتاج إضافة `liquidity_exit`.

2. **التصريف مربوط بالبوابة وحدها** — `drain_queue` يُستدعى من
   `set_gate` فقط. لو انتهى الفيتو والبوابة ما زالت STAYOUT، الطابور
   يفنى بصمت. يحتاج استدعاءً عند رفع الفيتو أيضاً.

3. **العمر 45 دقيقة قصير جداً** — الفيتو قد يمتد ساعات. حدث فعلياً
   اليوم: أربع إشارات دخلت 07:15، سقطت كلها قبل 11:43.

**الحل المقترح:** عمر أطول في حالة الفيتو (ساعتان مثلاً).

⚠️ **بثمن لازم يُدفع:** إشارة عمرها ساعتان تشير لسعر لم يعد قائماً.
فعند التصريف يُعاد فحص السعر الحالي، ومن ابتعد أكثر من عتبة معينة
(1%؟) يسقط. بلا هذا الفحص يدخل الوكيل على أسعار قديمة.

---

### 2. لوحة Master كاملة كبيانات  ⭐⭐ المشروع الكبير

**الهدف:** أن يرى الوكيل ما تراه أنت على اللوحة — لا أحداثاً متفرقة.

**الطريقة:** تنبيه واحد لكل عملة يحمل اللوحة كلها JSON بدل نص قصير:

```json
{"tk":"BTCUSDT.P","tf":"240","px":63054.9,"rsi":39,
 "cvd":-23340,"cvd_tr":-1,"rsi_tr":-1,"tools":0,
 "trig":0,"val":64770,"pos_val":-0.5,"absorb":0,
 "squeeze":0,"trap_long":1,"veto":1,"decision":0,
 "oi":3.38,"adx":26.7,"alts":-1}
```

**العمل المطلوب — ثلاث قطع منفصلة:**

1. **تعديل `Master_hunter.pine`** — إضافة `alert()` تبني JSON من
   المتغيرات القائمة. شغل Pine، يحتاج اختباراً دقيقاً.
2. **إعداد التنبيهات** — 25 عملة × الفريمات المطلوبة. الإعداد يدوي
   على كل تنبيه. مع الـ211 الحالية يبقى ضمن حد TradingView (400).
3. **طبقة قرار جديدة في الوكيل** — استقبال عشرين رقماً بلا قواعد
   تربطها لا يفيد. لازم كتابة الشروط صراحةً، مثل:
   - الزناد 0/3 + السعر عند VAL = امنع الشورت
   - الأدوات 0/7 = لا دخول مهما قالت البوابة
   - `trap_long` مفعّل = امنع اللونج ولو ارتد السعر

   ⚠️ **هذه القطعة هي العمل الحقيقي** — ترجمة قراءتك للوحة إلى
   شروط. القطعتان الأوليان سباكة، وهذه هي العقل.

**لماذا مؤجَّل:** قد يتبيّن بعد البند 1 أن أربع طبقات تكفي.
البدء بها الآن يعني بناء شهر عمل على تخمين.

---

### 3. حدّ منفصل للطابور

ظهر في الاختبار: الطابور استهلك حد الصفقات اليومي (3) في لحظة واحدة
عند فتح البوابة، بدل توزيعها على اليوم.

**القرار:** راقب أولاً. لو أزعجك فعلياً، الحل حدّ مستقل للطابور
لا تعديل الحد اليومي.

---

### 4. فحص عمق التصحيح  ⚠️ خلاف مفتوح

**موقف محمود:** القاع لا يحتاج علماً — المرساة واضحة (دعم · MA36 ·
ذروة بيع) وصانع السوق يدافع عن المستويات.

**موقف كلود:** الوكيل لا يرى ما تراه. وفلسفتك المكتوبة نفسها تقول
«BTC مصحّح + إشارات = يوم سعد · بلا تصحيح = حجم أصغر». بلا هذا
الفحص يعامل إشارة بعد هبوط 8% مثل إشارة بعد صعود متواصل.

**الحسم:** السجل بعد الأسبوع، لا الجدال.

**عائق تقني:** يحتاج RSI أو مسافة السعر من القاع — ولا يصل الوكيل
أيٌّ منهما. الرسائل تحمل `{{ticker}}` و`{{close}}` فقط. فهو مرتبط
بالبند 2 عملياً.

---

### 5. الإشارة المكررة — من توثيق إلى فعل

الآن تُوثَّق فقط (`repeat_signal`) وتُرفض. بعد الأسبوع، افحص كل سطر:

- `verdict: تأكيد محتمل` (سعر أفضل + بعد ساعة+) = كان تأكيداً وضاع
- `verdict: ⚠️ سكين محتملة` (سعر أسوأ + خلال 30د) = الرفض أنقذك

**لو التأكيدات كثيرة:** اجعلها ترفع أولوية الصفقة القائمة أو تؤجّل
الخروج. **لو السكاكين أكثر:** اترك السلوك كما هو.

العتبات الحالية (0% · 60 دقيقة · −0.5% · 30 دقيقة) مبدئية للفرز —
تُعدَّل على بياناتك الفعلية.

---

## 🔌 Pionex API — مسار منفصل

⚠️ **Futures API بدعوة فقط.** ابدأ بطلب الصلاحية.

أسئلة للدعم:
- كيف أحصل على الصلاحية؟
- حساب فرعي بمفتاح مستقل؟
- صلاحيات محددة (تداول بلا سحب)؟
- تقييد بعنوان IP؟

**التصميم جاهز:** `PaperBroker` طبقة منفصلة — إضافة Pionex =
استبدال كلاس واحد، بلا مساس بباقي الكود.

**المفاتيح لا تُكتب في الكود ولا تُشارك** — Environment في Render فقط.

---

## 🚫 قواعد ثابتة لا تُخالَف

- **لا تنفيذ حقيقي قبل أسبوع مراقبة كامل**
- الحكم على السباكة لا على الربح
- 100 صفقة لا تكفي — الرقم الحقيقي ~400
- الحدود: $1 · 25x · 3 صفقات يومياً · 3 مفتوحة · إيقاف عند −30%
- **لا تعديل بلا بيانات.** كل بند هنا يُحسم بالسجل لا بالإحساس.

---

## قاعدة الخروج المعتمدة (31 يوليو)

مطبَّقة في الكود، لا تُعدَّل قبل 30 صفقة موثّقة:

1. بلوغ TP حسب الرافعة → **أغلق النصف فوراً بلا تفكير**
2. انقل SL للنصف الباقي إلى **نقطة الدخول** — لا يمكن أن تخسر بعدها
3. الباقي يخرج على: انعكاس هيكن · أو زناد قمة 3/3 · أو انقلاب البوابة

⚠️ **ممنوع تحريك SL للأسفل بعد المرحلة 2** — هذا هو العطب نفسه
الذي بُنيت القاعدة لمنعه.
